name: 🚀 Deploy to cPanel

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: 📦 Install dependencies
        run: npm install

      - name: 🔨 Build Astro
        run: npx astro build
        env:
          CI: true
          NODE_OPTIONS: "--max-old-space-size=4096"

      - name: 🔎 Pagefind index
        run: npx pagefind --site dist

      - name: 📦 Compress dist
        run: tar -czf deploy.tar.gz -C dist .

      - name: 🔑 Setup SSH
        uses: webfactory/ssh-agent@v0.9.1
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: 🧹 Scan host key
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: 📤 Deploy to cPanel
        run: |
          # Upload compressed archive (1 file = fast)
          scp -P ${{ secrets.SSH_PORT }} deploy.tar.gz \
            ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.REMOTE_PATH }}/

          # Extract on server — overwrite existing files
          ssh -p ${{ secrets.SSH_PORT }} ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} \
            "cd ${{ secrets.REMOTE_PATH }} && tar -xzf deploy.tar.gz && rm deploy.tar.gz"
